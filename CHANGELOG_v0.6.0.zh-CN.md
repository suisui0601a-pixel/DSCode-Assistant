# DSCode Assistant v0.6.0

[English](CHANGELOG_v0.6.0.md)

## 新增

- 新增 Raw 上下文模式，保持请求消息的顺序和正文不变。
- 新增确定性 Light 上下文模式，可清理空消息、无效占位消息和安全范围内的连续重复消息。
- 新增连续短消息的有限合并，不进行语义改写。
- 新增关键上下文保护，包括系统指令、当前任务、最近有效回复、代码块、补丁、错误日志、明确约束和文件引用。
- 新增本地保护统计和聊天界面的上下文 Token 估算。

## 改进

- 将上下文准备接入 `ChatWidget` 与 `ChatWorker` 之间，同时保持模型提供商请求格式不变。
- 为上下文保护接入本地语言检测提示，不上传诊断信息。
- 保持旧设置文件兼容；缺少上下文设置时继续使用 Raw。
- 统一 Python 包和 Windows 构建元数据为 v0.6.0。

## 修复

- 上下文功能未新增单独的运行时修复声明。
- 现有回归测试继续覆盖消息生命周期、流式更新、模型提供商装配和数据库行为。

## 文档

- 将英文 README 作为 GitHub 默认入口，并提供完整的简体中文版本。
- 补充上下文优化、模型提供商、隐私、安装、配置和开发说明。
- 新增行为准则和隐私安全截图贡献说明。
- 新增 v0.6.0 公开发布报告。

## 兼容性说明

- Auto 为实验占位，v0.6.0 当前按 Raw 运行。
- Token 数为本地估算，不等同于模型提供商 usage 或账单数据。
- 本版本不新增数据库迁移。
- DeepSeek 请求流程、OpenAI 兼容接口和 `ChatWorker` 信号模型保持不变。
